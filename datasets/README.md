# Foreground-preservation dataset

Put labeled examples here. See [TRAINING.md](../TRAINING.md) for the split rules, mask layers, and metrics.

**Active training data** for the matte refiner lives in **`private/labels/`** (gitignored). Run `npm run train:bootstrap` while the API is up to fill it from `.test-images/`.

This folder is a template. It does not contain trained weights.
